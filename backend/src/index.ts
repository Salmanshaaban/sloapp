import express from 'express';nimport cors from 'cors';nconst app = express();napp.use(express.json());napp.use(cors({origin:'*',credentials:true,methods:['GET','POST','PUT','DELETE','OPTIONS'],allowedHeaders:['Content-Type','Authorization']}));napp.post('/api/auth/login',(req,res)=>{const{email,password}=req.body;if(email==='slman05088@gmail.com'){return res.json({token:'admin-token-ultimate',user:{email:'slman05088@gmail.com',role:'admin',name:'Admin'}});}return res.status(200).json({token:'user-token-mock',user:{email:email,role:'user',name:'User'}});});napp.get('/api/health',(req,res)=>res.json({status:'ok'}));
export default app;
